.env
.env.*
.netlify/
node_modules/
.DS_Store

dist/
.claude/

# ── Tool pipeline outputs (belt-and-suspenders; per-tool .gitignore handles exceptions) ──
# These are always regenerable artifacts — never commit them.
tools/*/output_json/
tools/*/reports/
